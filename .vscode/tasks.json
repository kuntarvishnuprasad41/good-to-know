{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C/C++: LearnCpp.cpp build active file",
      "command": "g++", // Change this to the compiler
      "args": [
        "-fdiagnostics-color=always",
        "-g", // Generate debug information
        "${file}", // Input source file
        "-o", // Output executable
        "${fileDirname}/${fileBasenameNoExtension}" // Output path and name
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Task generated by Debugger."
    }
  ]
}
